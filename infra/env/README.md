# Environment Configuration

> **Development in progress.** These files contain values for the active development environment. They should be excluded from the repository or replaced with placeholder values before publication.

| File | Purpose |
|------|---------|
| `dev.tfvars` | Terraform variable values for the dev environment |
| `dev.backend.hcl` | S3 backend configuration (state bucket, key path) |
